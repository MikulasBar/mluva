[workspace.package]
authors = ["Mikuláš Bartáček"]
version = "0.1.0"
edition = "2024"

[package]
name = "mluva"
authors.workspace = true
version.workspace = true
edition.workspace = true

# This is a virtual manifest
# No source code in this crate
[lib]
path = ""

[workspace]
members = [
    "cli",
    "common",
    "compiler",
    "frontend",
    "typechecker",
    "vm",
]

[workspace.dependencies]
codespan-reporting = "0.13.1"
bincode = "2.0.1"

common = { path = "common" }
frontend = { path = "frontend" }
typechecker = { path = "typechecker" }
compiler = { path = "compiler" }
vm = { path = "vm" }
